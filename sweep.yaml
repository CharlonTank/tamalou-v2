sandbox:
  install:
    # Download Lamdera binary for Linux x86_64 and set execute permissions
    - curl https://static.lamdera.com/bin/lamdera-1.2.1-linux-x86_64 -o /usr/local/bin/lamdera
    - chmod a+x /usr/local/bin/lamdera
    # Additional setup commands can go here, if necessary

  check:
    - lamdera make src/Frontend.elm src/Backend.elm

# Sweep AI turns bugs & feature requests into code changes (https://sweep.dev)
# For details on our config file, check out our docs at https://docs.sweep.dev/usage/config

# This setting contains a list of rules that Sweep will check for. If any of these rules are broken in a new commit, Sweep will create an pull request to fix the broken rule.
rules:
  - "If you see an easy improvment to the code, make a pull request to fix it."
  - "If you see a problem in the code, make a pull request to fix it."

# This is the branch that Sweep will develop from and make pull requests to. Most people use 'main' or 'master' but some users also use 'dev' or 'staging'.
branch: "master"

# By default Sweep will read the logs and outputs from your existing Github Actions. To disable this, set this to false.
# gha_enabled: False

# This is the description of your project. It will be used by sweep when creating PRs. You can tell Sweep what's unique about your project, what frameworks you use, or anything else you want.
#
# Example:
#
# description: sweepai/sweep is a python project. The main api endpoints are in sweepai/api.py. Write code that adheres to PEP8.
description: "WIP. Tamalou in a multiplayer card game. We use Lamdera which is a Frontend+Backend elm framework. We use elm 0.19.1. We use elm-ui for the frontend. The most used types are in src/Types.elm. The logic of the game is in src/Frontend.elm and src/Backend.elm. cardImages directory contains all the images of the cards."

# This sets whether to create pull requests as drafts. If this is set to True, then all pull requests will be created as drafts and GitHub Actions will not be triggered.
draft: False

# This is a list of directories that Sweep will not be able to edit.
blocked_dirs: ["src/cardImages"]
